module.exports = {
    testEnvironment: 'node', // Utilisation de l'environnement Node.js pour tester le backend
    verbose: true, // Affiche des détails sur les tests exécutés
    testMatch: ['**/tests/**/*.test.js'], // Les fichiers de test doivent être dans un dossier "tests"
  };